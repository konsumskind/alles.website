{
  "name": "janosch-kartschall-web",
  "private": true,
  "version": "0.0.3",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist",
    "build:templates": "node backend/build-templates.js"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "juice": "^11.1.1",
    "sass": "^1.70.0",
    "vite": "^5.0.0"
  }
}
